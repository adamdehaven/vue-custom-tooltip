import { App } from 'vue';
import { TooltipOptions } from './types';
import './tooltip.scss';
declare const _default: {
    install: (app: App, options?: TooltipOptions | undefined) => void;
};
export default _default;
